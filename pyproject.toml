[project]
name = "cv_shared"
version = "0.3.0"
description = "Shared utilities for computer vision projects"
requires-python = ">=3.11"
dependencies = ["opencv-python", "numpy"]

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
